Bonjour{{ pseudo ? ' ' ~ pseudo }},

Une réinitialisation du mot de passe de ton compte {{ config.siteName }} a été demandée.
Pour choisir un nouveau mot de passe, ouvre ce lien (valable {{ ttl }} minutes) :

{{ link }}

Si tu n'es pas à l'origine de cette demande, ignore cet email : ton mot de passe ne change pas.

— L'équipe {{ config.siteName }}
